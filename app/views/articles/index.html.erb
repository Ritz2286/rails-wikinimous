
<% @articles.each do |article| %>
<ul>
  <li class="list-unstyled">
    <%= link_to article.title, article_path(article.id), style: "color:#337ab7;", class: 'h4' %>
    <p class="text-muted">
    <%= article.content %></p>
  </li>
  </ul>
  <% end %>
  <hr>


