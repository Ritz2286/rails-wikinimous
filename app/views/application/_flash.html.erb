<% flash.each do |type, message| %>
  <div class="alert alert-info <%= bootstrap_class_for(type) %> fade show">
    <button class="close" data-dismiss="alert">×</button>
    <%= message %>
  </div>
<% end %>
